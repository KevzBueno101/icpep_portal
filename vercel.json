{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist",
  "rootDirectory": "frontend",
  "rewrites": [
    { "source": "/admin-portal/(.*)", "destination": "/index.html" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}

